@model IEnumerable<FootballWebApp.Models.red_cards>

@{
    ViewBag.Title = "Red Cards";
    Layout = "~/Views/Shared/_Layout.cshtml";
}



<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.match.date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.player.fullname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.team.name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.match.date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.player.fullname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.team.name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.red_card_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.red_card_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.red_card_id })
        </td>
    </tr>
}

</table>
